FROM maven:3.9-eclipse-temurin-17
WORKDIR /app
EXPOSE 8080
# Monta o projeto inteiro via volume no docker-compose.
# DevTools detecta mudanças nas classes compiladas pelo IDE (auto-compile habilitado).
CMD ["sh", "-c", "mvn install -DskipTests -q && mvn -pl apresentacao-backend spring-boot:run"]
